"""Analysis subpackage for the AI-trend paper.

This package holds the (public) analysis code — velocity/acceleration
computations, network metrics, rolling aggregations, and Δ-report
generators. The actual derived data these modules emit lives under
``data/research_private/`` and is gitignored so the paper's raw
findings stay unpublished until submission.

See ``research/README.md`` at the repo root for the paper direction
and ``research/methodology.md`` for every metric's formal definition.
"""
