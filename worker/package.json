{
  "name": "ai-daily-news-analytics-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "db:init": "wrangler d1 execute ai-daily-news-analytics --remote --file=schema.sql",
    "db:init:local": "wrangler d1 execute ai-daily-news-analytics --local --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.128.0"
  }
}
